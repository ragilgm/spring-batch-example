spring:
  jpa:
    database-platform: org.hibernate.dialect.MySQL5Dialect
    hibernate:      ddl-auto: create
    show-sql: true
    properties:      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver    url: jdbc:mysql://localhost:3306/springbatch?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: Ragil404*
  batch:    initialize-schema: ALWAYS
    job:      enabled: false
      
      
      
